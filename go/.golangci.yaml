# Options for analysis running.
run:
  # Timeout for analysis, e.g. 30s, 5m.
  # Default: 1m
  timeout: 5m

  # Which dirs to skip: issues from them won't be reported.
  # Can use regexp here: `generated.*`, regexp is applied on full path.
  # Default value is empty list,
  # but default dirs are skipped independently of this option's value (see skip-dirs-use-default).
  # "/" will be replaced by current OS file path separator to properly work on Windows.
  skip-dirs:
    - ecdh # temporary (till go1.20 release)

  # Which files to skip: they will be analyzed, but issues from them won't be reported.
  # Default value is empty list,
  # but there is no need to include all autogenerated files,
  # we confidently recognize autogenerated files.
  # If it's not please let us know.
  # "/" will be replaced by current OS file path separator to properly work on Windows.
  skip-files:
    - "^.*\\_test\\.go$"
    - "^.*\\_mock\\.go$"

linters:
  # Enable all available linters.
  # Default: false
  enable-all: true

  # Disable specific linter
  # https://golangci-lint.run/usage/linters/#disabled-by-default
  disable:
    - deadcode         # Replaced by unused.
    - varcheck         # Replaced by unused.
    - structcheck      # Replaced by unused.
    - golint           # Replaced by revive.
    - exhaustivestruct # Replaced by exhaustruct.
    - scopelint        # Replaced by exportloopref.
    - maligned         # Replaced by govet 'fieldalignment'.
    - nosnakecase      # Replaced by revive(var-naming).

#    - ifshort    # (since v1.48.0) The repository of the linter has been deprecated by the owner.
    - interfacer # (since v1.38.0) The repository of the linter has been archived by the owner.
    - exhaustruct # it's better to "Make the zero value useful <https://www.youtube.com/watch?v=PAAkCSZUG1c&t=6m25s>".

linters-settings:
  gci:
    # Section configuration to compare against.
    # Section names are case-insensitive and may contain parameters in ().
    # The default order of sections is `standard > default > custom > blank > dot`,
    # If `custom-order` is `true`, it follows the order of `sections` option.
    # Default: ["standard", "default"]
    sections:
      - standard # Standard section: captures all standard packages.
      - default # Default section: contains all imports that could not be matched to another section type.
      - prefix(github.com/itzoo-space) # Custom section: groups all imports with the specified Prefix.
      - blank # Blank section: contains all blank imports. This section is not present unless explicitly enabled.
      - dot # Dot section: contains all dot imports. This section is not present unless explicitly enabled.
